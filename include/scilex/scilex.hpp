/*!
 * \file scilex.hpp
 * \brief Single public header for SciLex — include this to use the library.
 *
 * SciLex is a small, header-only C++20 lexer built on REAL. It turns an
 * ordered set of token rules (each a `real::regex`) into a maximal-munch,
 * linear-time tokenizer. Pure, dependency-light (only REAL), and honest about
 * its scope.
 */
#ifndef SCILEX_SCILEX_HPP
#define SCILEX_SCILEX_HPP

#include "token.hpp"
#include "lexer.hpp"

/*!
 * \namespace scilex
 * \brief The SciLex public API (\ref scilex::lexer, \ref scilex::rule,
 *        \ref scilex::token).
 */

#endif // SCILEX_SCILEX_HPP
